<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CalculatorApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ButtonNumbers" parent="Theme.MaterialComponents.Light">
        <item name="backgroundTint">@color/numbers</item>
        <item name="cornerRadius">100dp</item>
        <item name="fontFamily">sans-serif-condensed-medium</item>
        <item name="android:textSize">30sp</item>
        <item name="rippleColor">@color/white</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ButtonOperators" parent="Theme.MaterialComponents.Light">
        <item name="backgroundTint">@color/operators</item>
        <item name="cornerRadius">100dp</item>
        <item name="fontFamily">sans-serif-condensed-medium</item>
        <item name="android:textSize">30sp</item>
        <item name="rippleColor">@color/white</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ButtonClear" parent="Theme.MaterialComponents.Light">
        <item name="backgroundTint">@color/clear</item>
        <item name="cornerRadius">100dp</item>
        <item name="fontFamily">sans-serif-condensed-medium</item>
        <item name="android:textSize">30sp</item>
        <item name="rippleColor">@color/white</item>
        <!-- Customize your theme here. -->
    </style>
</resources>